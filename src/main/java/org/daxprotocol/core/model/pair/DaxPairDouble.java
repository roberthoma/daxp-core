package org.daxprotocol.core.model.pair;

import org.daxprotocol.core.model.tag.DaxTag;

public class DaxPairDouble extends DaxPair<Double> {
    public DaxPairDouble(DaxTag tag, Double value){
        super(tag, value);
    }
}
